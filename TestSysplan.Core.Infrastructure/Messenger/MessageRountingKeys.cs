namespace TestSysplan.Core.Infrastructure.Messenger
{
    public static class MessageRountingKeys
    {
        public const string CLIENT_INSERTED = nameof(CLIENT_INSERTED);

        public const string CLIENT_DELETED = nameof(CLIENT_DELETED);
    }
}
